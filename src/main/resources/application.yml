server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/branch_db
    username: branch_user
    password: branch_pass

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
